<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Omar Faruq | Advanced Slideshow App</title>
<style>
  :root {
    --primary: #3b82f6;
    --dark-bg: #0f172a;
    --light-bg: #f8fafc;
    --dark-text: #e2e8f0;
    --light-text: #111827;
  }

  body {
    margin: 0;
    font-family: 'Poppins', sans-serif;
    background: var(--dark-bg);
    color: var(--dark-text);
    transition: background 0.6s, color 0.6s;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  header {
    text-align: center;
    padding: 1rem;
  }

  header h1 {
    color: var(--primary);
    margin: 0.3rem;
  }

  .carousel {
    position: relative;
    max-width: 900px;
    width: 90%;
    overflow: hidden;
    border-radius: 16px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.4);
  }

  .slide {
    display: none;
    width: 100%;
    height: 500px;
    position: relative;
  }

  .slide img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .caption {
    position: absolute;
    bottom: 20px;
    left: 20px;
    background: rgba(0,0,0,0.5);
    padding: 10px 20px;
    border-radius: 8px;
    font-size: 1.1rem;
  }

  .active {
    display: block;
    animation: fadeIn 1s ease-in-out;
  }

  @keyframes fadeIn {
    from {opacity: 0;}
    to {opacity: 1;}
  }

  .nav-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(0,0,0,0.5);
    color: white;
    font-size: 2rem;
    padding: 8px 16px;
    border-radius: 50%;
    cursor: pointer;
    border: none;
    transition: background 0.3s;
  }

  .nav-btn:hover { background: rgba(0,0,0,0.8); }
  .prev { left: 15px; }
  .next { right: 15px; }

  .dots {
    text-align: center;
    margin-top: 10px;
  }

  .dot {
    height: 12px;
    width: 12px;
    margin: 5px;
    background-color: #bbb;
    border-radius: 50%;
    display: inline-block;
    cursor: pointer;
  }

  .dot.active-dot { background-color: var(--primary); }

  .controls {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin: 15px;
  }

  button {
    background: var(--primary);
    color: white;
    border: none;
    padding: 10px 18px;
    border-radius: 10px;
    cursor: pointer;
    transition: 0.3s;
  }

  button:hover { background: #2563eb; }

 
  .light {
    background: var(--light-bg);
    color: var(--light-text);
  }
</style>
</head>
<body>

<header>
  <h1>Omar Faruq’s Advanced Slideshow</h1>
  <p> Project – Interactive Carousel App</p>
</header>

<div class="carousel">
  <div class="slide active">
    <img src="https://images.unsplash.com/photo-1506744038136-46273834b3fb" alt="Nature">
    <div class="caption">🌄 Beautiful Nature</div>
  </div>
  <div class="slide">
    <img src="https://images.unsplash.com/photo-1491553895911-0055eca6402d" alt="City">
    <div class="caption">🏙️ Urban Skyline</div>
  </div>
  <div class="slide">
    <img src="https://images.unsplash.com/photo-1493244040629-496f6d136cc3" alt="Mountains">
    <div class="caption">⛰️ Peaceful Mountains</div>
  </div>

  <button class="nav-btn prev">❮</button>
  <button class="nav-btn next">❯</button>
</div>

<div class="dots"></div>

<div class="controls">
  <button id="toggleTheme">🌗 Toggle Theme</button>
  <button id="pausePlay">⏸ Pause</button>
</div>

<script>
  const slides = document.querySelectorAll('.slide');
  const nextBtn = document.querySelector('.next');
  const prevBtn = document.querySelector('.prev');
  const dotsContainer = document.querySelector('.dots');
  const themeBtn = document.getElementById('toggleTheme');
  const pauseBtn = document.getElementById('pausePlay');
  let index = 0;
  let autoPlay = true;
  let theme = 'dark';

 
  slides.forEach((_, i) => {
    const dot = document.createElement('span');
    dot.classList.add('dot');
    if (i === 0) dot.classList.add('active-dot');
    dot.addEventListener('click', () => showSlide(i));
    dotsContainer.appendChild(dot);
  });

  const dots = document.querySelectorAll('.dot');

  function showSlide(n) {
    slides.forEach((slide, i) => {
      slide.classList.remove('active');
      dots[i].classList.remove('active-dot');
    });
    slides[n].classList.add('active');
    dots[n].classList.add('active-dot');
    index = n;
  }

  function nextSlide() {
    index = (index + 1) % slides.length;
    showSlide(index);
  }

  function prevSlide() {
    index = (index - 1 + slides.length) % slides.length;
    showSlide(index);
  }

  nextBtn.addEventListener('click', nextSlide);
  prevBtn.addEventListener('click', prevSlide);

 
  let interval = setInterval(() => {
    if (autoPlay) nextSlide();
  }, 4000);

 
  document.querySelector('.carousel').addEventListener('mouseenter', () => autoPlay = false);
  document.querySelector('.carousel').addEventListener('mouseleave', () => autoPlay = true);

 
  themeBtn.addEventListener('click', () => {
    document.body.classList.toggle('light');
    theme = theme === 'dark' ? 'light' : 'dark';
  });

 
  pauseBtn.addEventListener('click', () => {
    autoPlay = !autoPlay;
    pauseBtn.textContent = autoPlay ? '⏸ Pause' : '▶️ Play';
  });


  document.addEventListener('keydown', e => {
    if (e.key === 'ArrowRight') nextSlide();
    if (e.key === 'ArrowLeft') prevSlide();
  });
</script>

</body>
</html>
